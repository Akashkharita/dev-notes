# 2026-08-17

## 🔬 What I tested / ran

- Vendored QuakeXNet v3 pretrained weights (`base.pt.v3` and `base.json.v3`) directly into the `pnw_seismic_event_detection` repo, sourced from the SeisBench model hub. The model is loaded via `from_pretrained("base", version_str='3')` in the detection pipeline. Commit `d9af765`, pushed 00:05 UTC.
- Reorganized the `phasenet-retrain` repo: moved the v7 checkpoint out of `checkpoints/` (git-ignored training scratch space) and into `models/` for cleaner repo layout. Commit `77f581d`, pushed 00:13 UTC. Per advisor feedback.

## 📊 Results & findings

- `checkpoints/` in `phasenet-retrain` was accumulating 969 MB of experimental checkpoint variants — this is what motivated the move to a separate `models/` directory for deployment-ready artifacts.
- QuakeXNet v3 weights (`base.pt.v3` / `base.json.v3`) are now versioned alongside the pipeline code in `pnw_seismic_event_detection`.

## 💡 Hypotheses & interpretations

- Vendoring the model weights directly into the detection repo (rather than pulling from SeisBench hub at runtime) pins the deployed model to an exact version, reducing the risk of silent model changes breaking the production pipeline. Good practice before any wider deployment or paper submission.
- Separating `models/` (deployment-ready) from `checkpoints/` (training scratch, git-ignored) in `phasenet-retrain` enforces a clean distinction between "things we ship" and "training artifacts." This should also make it easier for collaborators (and advisors) to identify which checkpoint is canonical.

## ✅ Decisions made and why

- **Vendor QuakeXNet v3 weights into `pnw_seismic_event_detection`** rather than relying on a runtime `from_pretrained` call to the SeisBench hub. Reason: reproducibility and deployment stability — the pipeline code and model weights are now co-versioned in git.
- **Move v7 PhaseNet checkpoint to `models/`** rather than leaving it in `checkpoints/`. Reason: explicit advisor feedback; `checkpoints/` is git-ignored and not a reliable place to track a deployment-ready artifact. The 969 MB bloat in `checkpoints/` made it clear the scratch space was getting unwieldy.

## ❌ What failed / surprised

- Nothing to report. Both commits appear to be clean housekeeping/organizational pushes with no reported errors.

## 🚧 Blockers & open questions

- Nothing to report from today's activity specifically, but worth flagging: if `base.pt.v3` is a large binary file, vendoring it in git (rather than git-LFS) could become a repo size problem over time. Worth confirming whether git-LFS is configured for `pnw_seismic_event_detection`.
- Unclear whether `models/` in `phasenet-retrain` is now tracked in `.gitignore` or not — need to verify the v7 checkpoint is actually committed and not accidentally ignored.

## 📅 Tomorrow's priority

**Verify that the vendored QuakeXNet v3 weights load correctly end-to-end in the detection pipeline** (i.e., run a smoke test with `from_pretrained("base", version_str='3')` against real PNW waveforms to confirm the weights are intact and not corrupted by the git commit).

Supporting tasks:
1. Confirm git-LFS status for binary model files in `pnw_seismic_event_detection` — flag if large binaries are being stored naively in git history.
2. Verify `models/` directory is not accidentally caught by `.gitignore` in `phasenet-retrain`; confirm the v7 checkpoint is properly tracked.

## 🏷️ Topics

QuakeXNet, PhaseNet, SeisBench, PNSN, benchmark, waveform-processing

**TL;DR:** Housekeeping day — vendored QuakeXNet v3 weights into the detection repo for reproducible deployment, and relocated the canonical PhaseNet v7 checkpoint to `models/` per advisor feedback.

---
*Auto-generated · [GitHub](https://github.com/Akashkharita)*
