# 2026-06-01

## 🔬 What I tested / ran

- Iterated on `dev-notes` GitHub Actions workflow across ~9 commits (06:28–07:46 UTC): debugged PAT-based private repo access, added debug steps, manually added Denolle-Lab repos, tested direct repo access, and fixed index.html serving from root.
- Updated `notebooks/04_creating_benchmark_dataset.ipynb` in `Denolle-Lab/phasenet-retrain` (commit `7268af2`, 05:12 UTC).
- Enumerated all available pretrained weights for PhaseNet (16 variants) and EQTransformer (12 variants) via SeisBench.
- Inventoried SeisBench dataset cache: checked availability and sizes for 12+ datasets; confirmed `pisdl` is not in cache (SKIP).
- Computed P/S arrival coverage statistics across all cached datasets.
- Defined binning scheme for magnitude, depth, and distance distributions (JSON config).
- Generated and saved `physics_distributions.png` (two figures: 1800×500 and 1800×600).

---

## 📊 Results & findings

**SeisBench dataset inventory (traces with P/S coverage):**

| Dataset        | Total traces | Has P     | Has S     | Both      | % Both |
|----------------|-------------|-----------|-----------|-----------|--------|
| stead          | 1,265,657   | 1,030,231 | 1,030,231 | 1,030,231 | 81.4%  |
| ceed           | 4,140,530   | 4,140,530 | 4,140,530 | 4,140,530 | 100.0% |
| instancecounts | 1,159,249   | 1,159,249 | 713,883   | 713,883   | 61.6%  |
| pnw            | 183,909     | 183,909   | 183,909   | 183,909   | 100.0% |
| txed           | 519,689     | 312,231   | 312,231   | ~60.1%    | ~60.1% |
| ethz           | 36,743      | —         | —         | —         | —      |
| obst2024       | 60,394      | —         | —         | —         | —      |
| mlaapde        | 228,256     | —         | —         | —         | —      |
| vcseis         | 160,278     | —         | —         | —         | —      |
| cwa            | 346,959     | —         | —         | —         | —      |
| aq2009gm       | 258,984     | —         | —         | —         | —      |
| geofon         | 275,274     | —         | —         | —         | —      |

- `pisdl`: **SKIP** — not in cache.
- `ceed` (616.6 GB) and `crew` (1151.9 GB) are the largest datasets by size; `stead` is 28.1 GB-class.
- `stead` STEAD magnitude distribution (from JSON): peak in M0–M1 bin (347,189 traces) and M1–M2 bin (411,465 traces).
- Physics distribution plots saved to `physics_distributions.png`.

---

## 💡 Hypotheses & interpretations

- `ceed` (100% P+S coverage, 4.14M traces) will likely dominate any pooled benchmark dataset by raw count — need to consider subsampling or weighting to avoid ceed-dominated magnitude/depth bins.
- `instancecounts` has ~38.4% traces missing S picks — may introduce bias if S-phase recall is a benchmark metric; worth flagging or filtering.
- The magnitude binning (`-2` to `10` in integer steps) and depth binning (`0–5–10–20–35–70–150–300–700 km`) reflect a geophysically motivated design that should capture crustal vs. subduction zone depth regimes relevant to Cascadia.
- Distance bins (0–10–30–100–300–1000–5000–20000 km) span local to teleseismic — confirms the benchmark is intended to test model generalization across regimes, not just regional.
- The `dev-notes` workflow complexity (9 commits to fix PAT + private repo access) suggests GitHub Actions PAT scoping for org-level private repos is non-trivial; worth documenting the final working config.

---

## ✅ Decisions made

- Benchmark dataset notebook (`04_creating_benchmark_dataset.ipynb`) will use the defined JSON binning config (mag/depth/dist bins) as the canonical stratification scheme.
- `pisdl` excluded from the current benchmark build (not in cache).
- Both PhaseNet and EQTransformer pretrained weight sets will be evaluated in the benchmark (16 and 12 variants respectively).
- `physics_distributions.png` adopted as the standard dataset characterization figure.

---

## 🚧 Blockers & open questions

- `pisdl` dataset not in Cascadia HPC cache — unclear if this needs to be downloaded or can be excluded from benchmark.
- `instancecounts` S-pick incompleteness (~38.4% missing S): decision needed on whether to include only traces with both P+S, or use P-only traces as a separate stratum.
- `txed` P/S coverage stats appear truncated in output — need to verify full table.
- `dev-notes` workflow: unclear if private Denolle-Lab repo access is fully resolved after 9 commits — needs a clean end-to-end test.
- `crew` (1151.9 GB) is the largest dataset — not yet confirmed as loaded/accessible in cache.

---

## 📅 Plan for tomorrow

1. **Verify `pisdl` cache status** on Cascadia HPC; download or formally exclude from benchmark with documented rationale.
2. **Complete P/S coverage table** for all 12+ datasets (fill in missing rows for ethz, obst2024, mlaapde, vcseis, cwa, aq2009gm, geofon) and decide inclusion/exclusion criteria for S-incomplete datasets.
3. **Run stratified sampling** across mag/depth/dist bins to construct the balanced benchmark split; check for bin sparsity in combined dataset.
4. **Confirm `dev-notes` workflow** is fully operational with a single clean push and verify Denolle-Lab private repos appear correctly in the index.

---

## 🏷️ Topics
PhaseNet, SeisBench, benchmark, Cascadia-HPC, dataset-download, waveform-processing, visualization, recall-analysis

**TL;DR:** Built the benchmark dataset characterization notebook for PhaseNet retraining, inventorying 12+ SeisBench datasets totaling >8M traces with P/S coverage stats and physics distribution plots, while also debugging the dev-notes GitHub Actions workflow for private repo access.

---
*Auto-generated · [GitHub](https://github.com/Akashkharita)*
