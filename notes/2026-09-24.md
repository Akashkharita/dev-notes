# 2026-09-24

## 🔬 What I tested / ran

Committed SU picker data preparation and training plan to the `Denolle-Lab/surfcat` repository (commit `b0065f7`, pushed ~00:15 UTC, authored 2026-09-23). This represents planning and scaffolding work for training a dedicated SU (surface event) picker — likely a PhaseNet-style phase picker fine-tuned or adapted for surface event waveforms from the 25-year Pacific Northwest surface event catalog. Co-authored with Claude Opus 5.5 (1M context).

No HPC jobs reported submitted today. No benchmark runs or QuakeXNet inference reported.

## 📊 Results & findings

No quantitative experimental results to report today. The commit is infrastructure/planning in nature — data prep pipeline and training plan documentation for the SU picker. No metrics, loss curves, or evaluation numbers were produced.

## 💡 Hypotheses & interpretations

The decision to build a dedicated SU picker (rather than relying solely on the QuakeXNet classifier) suggests an emerging working hypothesis: **classification alone is insufficient, and phase-level detection tuned for surface events may improve recall or locatability of SU events** in the Pacific Northwest catalog. The existence of a 25-year surface event catalog in `surfcat` provides a substantial labeled training pool that could make a fine-tuned SU picker viable. Worth noting explicitly for future reference.

## ✅ Decisions made and why

- **Decided to scope out SU picker data prep and training plan as a discrete committed artifact.** Committing a training plan (rather than just keeping it in notes) creates a versioned, reviewable document — good practice before touching Cascadia HPC resources for a new training run. Ensures advisors (Denolle, Hutko) can review the approach before compute is spent.

## ❌ What failed / surprised

Nothing to report. The commit landed cleanly. No reported failures or surprises today.

## 🚧 Blockers & open questions

- **Training plan is written but not yet executed** — unclear if data prep scripts are fully tested end-to-end or just scaffolded.
- Open question: What is the exact SU training set size being drawn from the 25-year `surfcat` catalog, and how is it being split/balanced against non-SU examples?
- Open question: Is the SU picker intended to replace or complement QuakeXNet's SU class, or feed into an enveloc-style pipeline as a pre-filter?
- Open question: Will waveforms be pulled from PNSN archives directly, or are preprocessed waveform caches already available?

## 📅 Tomorrow's priority

**Run the SU picker data prep pipeline end-to-end on a small test subset and verify output waveform shapes, label distributions, and any data gaps.**

Supporting tasks:
1. Confirm training plan parameters (architecture choice, input window length, number of SU vs. non-SU examples) are documented and socialized with Denolle.
2. Check whether a SeisBench-compatible dataset wrapper is needed for the SU picker training data, and if so, stub it out.

## 🏷️ Topics

PhaseNet, QuakeXNet, SeisBench, PNSN, Mount-Rainier-catalog, waveform-processing, dataset-download, labelerrors

**TL;DR:** Committed a SU picker data prep and training plan to `surfcat`, laying the groundwork for a dedicated surface-event phase picker trained on the 25-year Pacific Northwest catalog.

---
*Auto-generated · [GitHub](https://github.com/Akashkharita)*
