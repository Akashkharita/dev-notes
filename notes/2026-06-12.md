# 2026-06-12

## 🔬 What I tested / ran

Evaluated `finetune_jma_wc_global_v3` on the full PhaseNet benchmark suite. This is the third version of the fine-tuned model built on the `jma_wc` parent checkpoint, retrained with SeisBench for cloud computing (Denolle-Lab/phasenet-retrain). The evaluation covered all 19 models currently in the benchmark comparison. Commit `eb1a680` pushed at 00:44 UTC.

---

## 📊 Results & findings

`finetune_jma_wc_global_v3` benchmark results across all 19 evaluated models:

| Metric | `finetune_jma_wc_global_v3` | `jma_wc` parent |
|---|---|---|
| P-MAE | **0.368 s** | 0.374 s |
| Recall | 87.2% | — |
| MCC | 0.747 | — |

- **P-MAE of 0.368 s is the best across all 19 models evaluated** — narrowly outperforms the `jma_wc` parent by 0.006 s.
- Recall (87.2%) and MCC (0.747) are described as "competitive" — not best-in-class on those metrics but not regressed either.

---

## 💡 Hypotheses & interpretations

- Fine-tuning `jma_wc` on the global v3 dataset is yielding real, measurable improvement in P-phase timing accuracy — the 0.006 s P-MAE improvement over the parent is small in absolute terms but meaningful if it holds across the full benchmark distribution and generalizes to Pacific Northwest data.
- The fact that recall (87.2%) and MCC (0.747) are "competitive" but not leading suggests the fine-tuning is tightening phase picks on events that are already being detected, rather than substantially expanding the detection envelope. This is a reasonable trade-off if the downstream use case (e.g., enveloc, Mount Rainier catalog) prioritizes precise pick times over raw recall.
- With 19 models now in the comparison, `finetune_jma_wc_global_v3` sitting at the top on P-MAE is a meaningful signal that the `jma_wc` lineage is the right parent to continue iterating on.

---

## ✅ Decisions made and why

- **Continued the `jma_wc` fine-tuning lineage to v3** — the parent model already showed strong P-MAE, and v3 confirms that fine-tuning on the global dataset meaningfully improves timing accuracy without sacrificing recall. This lineage is now the benchmark leader on P-MAE.
- **Benchmarked against all 19 models** — keeping the full comparison set ensures the v3 result isn't being evaluated in a vacuum; it's a true head-to-head ranking.

---

## ❌ What failed / surprised

- Nothing explicitly reported as failing today. The result was clean and directionally as expected.
- Worth noting: the improvement in P-MAE over the parent is narrow (0.006 s). Not a surprise, but worth watching — further fine-tuning iterations may hit diminishing returns on this metric.

---

## 🚧 Blockers & open questions

- Recall (87.2%) and MCC (0.747) are reported as "competitive" but the notebook doesn't have the full leaderboard values for those metrics across all 19 models. **Open question: which models lead on recall and MCC, and is there a Pareto-optimal model that beats v3 on those while staying close on P-MAE?**
- Unclear whether `finetune_jma_wc_global_v3` has been evaluated on PNSN/Pacific Northwest held-out data specifically, or only on the general SeisBench benchmark datasets. Generalization to local Cascadia waveforms (including SU events, quarry blasts) remains to be confirmed.
- No S-MAE numbers reported — is S-phase performance being tracked for this model?

---

## 📅 Tomorrow's priority

**Run a full leaderboard summary table for all 19 models across P-MAE, S-MAE, recall, and MCC to identify whether `finetune_jma_wc_global_v3` is Pareto-optimal or if there's a better balanced candidate.**

Supporting tasks:
1. Evaluate `finetune_jma_wc_global_v3` on a PNSN/Pacific Northwest–specific held-out split to test Cascadia generalization.
2. Check whether a v4 fine-tuning run is warranted or if v3 is the candidate to carry forward into the enveloc/Mount Rainier pipeline.

---

## 🏷️ Topics

PhaseNet, SeisBench, benchmark, recall-analysis, waveform-processing, Cascadia-HPC, Mount-Rainier-catalog, enveloc

---

**TL;DR:** `finetune_jma_wc_global_v3` achieves the best P-MAE (0.368 s) across all 19 benchmarked PhaseNet models, narrowly beating its `jma_wc` parent (0.374 s) with competitive recall (87.2%) and MCC (0.747).

---
*Auto-generated · [GitHub](https://github.com/Akashkharita)*
