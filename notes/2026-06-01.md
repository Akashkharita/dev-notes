# 2026-06-01

## 🔬 What I tested / ran

- Worked on `notebooks/04_creating_benchmark_dataset.ipynb` in **Denolle-Lab/phasenet-retrain**, committed as `[7268af2]` at 05:12 UTC ("updating the benchmark dataset notebook").
- Enumerated all available **PhaseNet pretrained weights** (16 models: diting, ethz, geofon, instance, iquique, jma, jma_wc, lendb, neic, obs, original, phasenet_sn, pisdl, scedc, stead, volpick) and **EQTransformer pretrained weights** (12 models: ethz, geofon, instance, iquique, lendb, neic, obs, original, original_nonconservative, scedc, stead, volpick) via SeisBench.
- Audited the SeisBench dataset cache for size and metadata availability. Notable sizes: crew (~1151.9 GB), cwa (~185.1 GB), ceed (~616.6 GB), instancecounts (~168.1 GB), aq2009gm (~28.1 GB), geofon (~27.8 GB), ethz (~23.3 GB).
- Scanned metadata schemas for multiple datasets (stead, ceed, instancecounts, pnw, txed, ethz, obst2024, mlaapde, vcseis, cwa, aq2009gm, geofon), checking fields for P/S arrivals, magnitude, depth, distance, and time.
- Computed **P/S arrival completeness statistics** across all loaded datasets.
- Defined binning scheme for the benchmark dataset: magnitude bins `[-2,-1,0,1,2,3,4,5,6,7,10]`, depth bins `[0,5,10,20,35,70,150,300,700]` km, distance bins `[0,10,30,100,300,1000,5000,20000]` km/deg.
- Generated and saved `physics_distributions.png` — two figures (1800×500 with 3 axes; 1800×600 with 3 axes) showing distributions across mag/depth/distance bins per dataset.
- Spent significant effort on **dev-notes repo infrastructure**: 14 commits across the day fixing GitHub Actions workflows, PAT authentication for private repo access, repo API params, adding Denolle-Lab repos manually, adding a daily brain dump form, and updating the note generator. Commits span 00:01–16:36 UTC.

---

## 📊 Results & findings

**Dataset sizes (from cache audit):**
| Dataset | Size (GB) | Has metadata |
|---|---|---|
| crew | 1151.9 | True |
| ceed | 616.6 | True |
| cwa | 185.1 | True |
| instancecounts | 168.1 | True |
| aq2009gm | 28.1 | True |
| geofon | 27.8 | True |
| ethz | 23.3 | True |
| dummydataset | 0.0 | True |

**P/S arrival completeness (from cache load):**
| Dataset | Total | Has P | Has S | Both | % Both |
|---|---|---|---|---|---|
| stead | 1,265,657 | 1,030,231 | 1,030,231 | 1,030,231 | **81.4%** |
| ceed | 4,140,530 | 4,140,530 | 4,140,530 | 4,140,530 | **100.0%** |
| instancecounts | 1,159,249 | 1,159,249 | 713,883 | 713,883 | **61.6%** |
| pnw | 183,909 | 183,909 | 183,909 | 183,909 | **100.0%** |
| txed | 519,689 | 312,231 | 312,2xx | 312,2xx | ~60% |
| ethz | 36,743 | — | — | — | — |
| obst2024 | 60,394 | — | — | — | — |
| mlaapde | 228,256 | — | — | — | — |
| vcseis | 160,278 | — | — | — | — |
| cwa | 346,959 | — | — | — | — |
| aq2009gm | 258,984 | — | — | — | — |
| geofon | 275,274 | — | — | — | — |

**pisdl: SKIP — not in cache.**

**Magnitude bin distribution for stead (from JSON):**
- M[-2,-1]: 0, M[-1,0]: 242, M[0,1]: 347,189, M[1,2]: 411,465, M[2,3]: 167,233, M[3,4]: 80,156, M[4,5]: 21,911, M[5,6]: 1,889, M[6,7]: 143, M[7,10]: 3

**PhaseNet pretrained weight count:** 16 models  
**EQTransformer pretrained weight count:** 12 models  
**Figures saved:** `physics_distributions.png` (two panel figures: 1800×500 and 1800×600)

---

## 💡 Hypotheses & interpretations

- **ceed is the largest fully-complete dataset** (4.14M traces, 100% P+S) and will likely dominate any stratified benchmark sample unless explicitly downweighted or capped per bin.
- **instancecounts has poor S-arrival completeness (61.6%)** — only 713,883 of 1,159,249 traces have both P and S. This is a meaningful limitation for any benchmark requiring S picks; traces without S will need to be filtered or treated as P-only samples.
- **stead's 81.4% completeness** is acceptable but the magnitude distribution is heavily concentrated in M[1,2] (411K) and M[0,1] (347K) — the benchmark will need care to avoid over-representing this range.
- **pisdl not being in cache** is a gap — it's one of the PhaseNet pretrained weight targets and its absence means we can't characterize its distribution yet.
- The distance bins spanning out to 20,000 km/deg confirm the benchmark is designed to cover teleseismic distances, consistent with the broader PhaseNet generalization goal.
- The dev-notes automation work (GitHub Actions + PAT) is overhead but will pay off if the daily note generation becomes reliable — the number of fixes today (14 commits) suggests the workflow was substantially broken and is now closer to stable.

---

## ✅ Decisions made and why

- **Binning scheme finalized** as mag `[-2…10]`, depth `[0…700 km]`, distance `[0…20,000]` — these ranges cover the full realistic range of seismicity from very shallow/local to deep/teleseismic, enabling stratified sampling that isn't biased toward the dominant regimes in any single training dataset.
- **Kept ceed and stead as primary datasets** in the benchmark inventory despite their size imbalance, because both have 100% and 81.4% P+S completeness respectively, making them the most usable for a phase-picking benchmark.
- **Decided to skip pisdl** for now since it is not in cache — flagged as a gap but not blocking the rest of the notebook.
- **Generated and saved `physics_distributions.png`** as a diagnostic artifact to visualize coverage before stratified sampling — good practice to catch bin sparsity early.
- **Chose to add Denolle-Lab repos manually** in dev-notes workflow (commit `[fdc322d]`) rather than relying on org-level API discovery, apparently because the automated org repo fetch wasn't surfacing private/org repos correctly.

---

## ❌ What failed / surprised

- **pisdl is not in cache** — this is a dataset targeted by one of the PhaseNet pretrained models but can't be characterized yet. Surprising given the others were available.
- **instancecounts S-arrival completeness is only 61.6%** — lower than expected for a dataset of this size and common use in SeisBench benchmarks. This limits its utility for joint P+S evaluation.
- **txed arrival counts appear truncated in the notebook output** ("312,2..." cut off) — exact Both/% figures not fully visible; needs re-check.
- **dev-notes GitHub Actions workflow required 14 commits over the full day to stabilize** — private repo access via PAT, org repo API params, and the brain dump form all needed iterative debugging. This was a significant time sink.

---

## 🚧 Blockers & open questions

- **pisdl not in cache** — need to download it or determine if it's publicly accessible via SeisBench. Blocking full dataset coverage in the benchmark.
- **txed arrival stats are partially truncated** in notebook output — need to re-run or scroll to get exact Both/% numbers.
- **Completeness stats for ethz, obst2024, mlaapde, vcseis, cwa, aq2009gm, geofon** were loaded (trace counts confirmed OK) but P/S breakdown wasn't printed in the captured output — unclear whether these were computed or just skipped.
- **Is the dev-notes GitHub Actions workflow now fully stable?** Commit `[5a20635]` at 16:36 UTC is the last fix ("fix repo API params") — unclear if tomorrow's automated run will succeed without further intervention.
- **Open design question:** How many traces per bin are needed for a statistically robust benchmark? No explicit target number has been set yet.
- **crew dataset (1151.9 GB)** — enormous; unclear if it will be included in stratified sampling or only used selectively given storage/compute constraints on Cascadia HPC.

---

## 📅 Tomorrow's priority

**Primary:** Run stratified sampling across all available datasets using the finalized mag/depth/distance bins and verify per-bin counts — identify which bins are sparse or empty before committing to a benchmark schema.

**Supporting tasks:**
1. Download or locate pisdl to fill the cache gap; check

---
*Auto-generated · [GitHub](https://github.com/Akashkharita)*
