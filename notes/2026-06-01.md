# 2026-06-01

## 🔬 What I tested / ran

- Worked on `notebooks/04_creating_benchmark_dataset.ipynb` in `Denolle-Lab/phasenet-retrain` — updated the benchmark dataset construction notebook
- Enumerated available pretrained weights for **PhaseNet** (16 variants: diting, ethz, geofon, instance, iquique, jma, jma_wc, lendb, neic, obs, original, phasenet_sn, pisdl, scedc, stead, volpick) and **EQTransformer** (12 variants)
- Audited SeisBench dataset cache: checked trace counts, metadata availability, and P/S arrival label completeness across all datasets in cache
- Computed and tabulated P/S arrival coverage statistics per dataset
- Defined stratification bins: magnitude (−2 to 10, 11 bins), depth (0–700 km, 8 bins), source–station distance (0–20,000 km, 7 bins)
- Generated and saved physics distribution figures (`physics_distributions.png`): magnitude, depth, and distance distributions across benchmark datasets (two figures: 1800×500 and 1800×600)
- Worked on `dev-notes` GitHub Actions workflows throughout the morning: iterated on PAT-based private repo access, added debug steps, and manually added Denolle-Lab repos to the automated notes index

## 📊 Results & findings

**Dataset cache audit (trace counts):**

| Dataset        | Total traces | Has P       | Has S       | Both        | % Both |
|----------------|-------------|-------------|-------------|-------------|--------|
| stead          | 1,265,657   | 1,030,231   | 1,030,231   | 1,030,231   | 81.4%  |
| ceed           | 4,140,530   | 4,140,530   | 4,140,530   | 4,140,530   | 100.0% |
| instancecounts | 1,159,249   | 1,159,249   | 713,883     | 713,883     | 61.6%  |
| pnw            | 183,909     | 183,909     | 183,909     | 183,909     | 100.0% |
| txed           | 519,689     | 312,231     | 312,2xx     | —           | ~60%   |
| ethz           | 36,743      | —           | —           | —           | —      |
| obst2024       | 60,394      | —           | —           | —           | —      |
| mlaapde        | 228,256     | —           | —           | —           | —      |
| vcseis         | 160,278     | —           | —           | —           | —      |
| cwa            | 346,959     | —           | —           | —           | —      |
| aq2009gm       | 258,984     | —           | —           | —           | —      |
| geofon         | 275,274     | —           | —           | —           | —      |
| **pisdl**      | —           | —           | —           | —          | **SKIP — not in cache** |

- `ceed` is by far the largest dataset at 4.14M traces with 100% P+S coverage
- `instancecounts` has a notable S-label gap: only 61.6% of traces have both P and S
- `pisdl` is missing from cache and will need to be downloaded separately
- `stead` magnitude distribution peaks in the M1–M3 range (M1: 242, M2: 347,189, M3: 411,465, M4: 167,233)
- Physics distribution plots saved successfully as `physics_distributions.png`

## 💡 Hypotheses & interpretations

- The S-label gap in `instancecounts` (38.4% of traces missing S picks) could introduce bias in any benchmark that assumes both phases are present — need to decide whether to subset or handle asymmetrically
- `ceed` dominates the combined dataset by raw count (~54% of all traces if pooled); this risks geographic/style bias toward Chinese seismicity unless subsampled or weighted
- The stratification bins defined today (mag/depth/dist) appear designed to enable stratified sampling for a geographically/physically balanced benchmark — this is the right approach given the ceed imbalance
- `dev-notes` automation issues with private repos suggest the GitHub Actions PAT scope or org-level permissions need review; the manual fallback (adding Denolle-Lab repos explicitly) works but is not scalable

## ✅ Decisions made

- Proceeding with the multi-dataset benchmark pool including: stead, ceed, instancecounts, pnw, txed, ethz, obst2024, mlaapde, vcseis, cwa, aq2009gm, geofon (12 datasets confirmed in cache)
- Stratification scheme locked in: magnitude bins [−2,−1,0,1,2,3,4,5,6,7,10], depth bins [0,5,10,20,35,70,150,300,700 km], distance bins [0,10,30,100,300,1000,5000,20000 km]
- `pisdl` will be skipped for now (not in cache)
- Denolle-Lab repos added manually to `dev-notes` as a workaround for PAT/private access issue

## 🚧 Blockers & open questions

- **`pisdl` not in cache** — need to determine if it should be downloaded and added to the benchmark, or excluded permanently
- **`instancecounts` S-label gap** — 38.4% missing S labels; unclear whether to (a) exclude S-only metrics for this dataset, (b) subset to traces with both labels, or (c) use asymmetric loss/evaluation
- **`dev-notes` GitHub Actions** — PAT-based access to private Denolle-Lab repos not working cleanly; root cause (PAT scope vs. org SSO vs. Actions permissions) not yet resolved
- **txed arrival counts** — output was truncated; full P/S/Both counts and % not confirmed
- What subsampling strategy to apply across datasets to avoid `ceed` dominating the benchmark distribution?

## 📅 Plan for tomorrow

1. **Resolve `instancecounts` S-label gap** — decide on subsetting strategy and implement a filtered trace list for benchmark construction
2. **Implement stratified sampling** using the locked magnitude/depth/distance bins to draw a balanced benchmark subset across all 12 datasets
3. **Download or confirm exclusion of `pisdl`** — check cache path and dataset size, decide if worth adding
4. **Fix `dev-notes` GitHub Actions PAT scope** — audit token permissions and org SSO settings; move to a working automated solution rather than manual repo addition

## 🏷️ Topics
PhaseNet, SeisBench, benchmark, dataset-download, waveform-processing, Cascadia-HPC, recall-analysis

**TL;DR:** Completed a full audit of the SeisBench benchmark dataset pool (12 datasets, ~8.6M total traces), locked in stratification bins for balanced sampling, and identified `instancecounts` S-label gap (61.6% coverage) and missing `pisdl` as key issues to resolve before benchmark construction.

---
*Auto-generated · [GitHub](https://github.com/Akashkharita)*
