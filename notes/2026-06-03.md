# 2026-06-03

## 🔬 What I tested / ran

Ran full inference evaluation pipeline (`notebooks/06_step_3_inference_evaluation.ipynb`, commit `ba9d07a`, 00:20 UTC) for the PhaseNet SeisBench generalization benchmark. Evaluated **16 PhaseNet weight variants** across 4 tiers on **32,144 benchmark traces** spanning 11 datasets.

- **Hardware:** NVIDIA GeForce RTX 3090, batch size 64, CUDA
- **Thresholds:** P/S detection threshold = 0.3 / 0.3; outlier threshold = ±1.5 s (Münchmeyer et al. 2022 uniform definition)
- **Weight tiers evaluated:**
  - Tier A: `stead`, `instance`, `neic`
  - Tier B: `diting`, `obs`, `volpick`, `pisdl`, `phasenet_sn`, `jma`, `jma_wc`
  - Tier C: `scedc`, `ethz`, `iquique`, `lendb`, `original`
  - Tier D: `geofon`
- All 16 weights verified as available before inference.
- Metrics computed across 3 splits: `all`, `cross_domain`, `in_domain`.
- Output saved to `step3_metrics.csv` → **144 metric rows** total.
- Applied normalization per model: `peak` (global, preserves Z/N/E component ratios) for `stead`, `instance`, `neic`, `volpick`; `std` (per-component) for `diting`, `obs`.

---

## 📊 Results & findings

**Benchmark dataset composition (32,144 traces total):**

| Dataset | Traces |
|---|---|
| stead | 9,366 |
| instancecounts | 8,406 |
| pnw | 2,333 |
| ceed | 4,530 |
| txed | 2,267 |
| mlaapde | 1,444 |
| aq2009gm | 1,363 |
| ethz | 1,365 |
| pisdl | 762 |
| cwa | 195 |
| vcseis | 113 |

**NaN audit:**
- `p_arrival_sample`: 43 NaN (expected — S-only or non-MLAAPDE traces)
- `s_arrival_sample`: 2,338 NaN (expected)
- `source_month`: 30,700 NaN (expected for most datasets)

**Inference errors:** 0 errors across all 16 models (32,144 ok each)

**Degenerate models (flat output — effectively non-functional):** `diting`, `original`, `phasenet_sn`

**In-domain P-phase performance** (model's training data present in benchmark):

| Weight | Tier | n_traces | p_med_prob | p_mae_s | mcc |
|---|---|---|---|---|---|
| stead | A | 9,366 | 0.9777 | 0.0916 | 0.9381 |
| instance | A | 8,406 | 0.9099 | 0.1333 | 0.9625 |
| ethz | C | 1,365 | 0.9352 | 0.1424 | 0.8964 |
| neic | A | 1,444 | *(truncated in output)* | — | — |

**Cross-domain performance — all distances** (degenerate models excluded, outlier thr = ±1.5 s):

| Tier | Weight | n_traces | p_recall | p_med_prob | mcc | p_mae_s | s_mae_s | p_outlier |
|---|---|---|---|---|---|---|---|---|
| A | instance | 23,738 | 0.841 | 0.837 | 0.851 | 0.453 | 1.660 | 0.090 |
| A | neic | *(truncated)* | — | — | — | — | — | — |

- Output CSV: `step3_metrics.csv`, 144 rows, 3 splits × 16 models × metrics.

---

## 💡 Hypotheses & interpretations

- **Degenerate model behavior** in `diting`, `original`, and `phasenet_sn` is a critical flag — flat outputs suggest these weights either failed to load correctly, have a normalization mismatch upstream (e.g., the `std` vs. `peak` preprocessing distinction may not have been applied correctly for `diting`), or the SeisBench wrapper is not invoking them as intended. Worth investigating separately before drawing any conclusions about their generalization.
- **In-domain stead performance** (p_mae_s = 0.0916 s, mcc = 0.9381) is strong and serves as a reasonable upper-bound ceiling for what a well-matched model can achieve on its own training distribution — useful for interpreting cross-domain degradation.
- **Cross-domain p_mae_s for `instance` jumps to 0.453 s** from in-domain 0.1333 s — nearly a 3.4× degradation in P-phase timing accuracy, suggesting significant distribution shift. The s_mae_s of 1.660 s cross-domain (close to the 1.5 s outlier threshold) implies S-phase picks are near the edge of what's considered usable.
- The `p_outlier` rate of 0.090 for `instance` cross-domain means ~9% of picks fall outside ±1.5 s — worth tracking whether this correlates with specific datasets (e.g., `pnw`, `vcseis`) or distance ranges.
- The 30,700 NaN values in `source_month` suggest temporal analysis (seasonal generalization) will be limited to a small subset of traces.

---

## ✅ Decisions made and why

- **Adopted Münchmeyer et al. 2022 outlier threshold (±1.5 s)** as the uniform benchmark standard — ensures comparability with published literature and is explicitly cited in the notebook.
- **Excluded degenerate models** (`diting`, `original`, `phasenet_sn`) from the cross-domain performance table — correct decision to avoid misleading metrics; flagged for separate investigation rather than silently dropped.
- **Normalization applied per-model** (`peak` vs. `std`) based on training dataset convention rather than using a single global norm — this was deliberate to faithfully reproduce each model's inference conditions. This is important: `diting` and `obs` use per-component `std`, while Tier A models use global `peak`.
- **Committed at 00:20 UTC** — notebook state captured in `ba9d07a` under `Denolle-Lab/phasenet-retrain`.

---

## ❌ What failed / surprised

- **Three models produced flat/degenerate output:** `diting`, `original`, `phasenet_sn`. This is unexpected and concerning — `diting` in particular is a Tier B model and should be functional. The fact that `diting` uses `std` normalization while the others use `peak` makes it a candidate for a preprocessing bug, but `phasenet_sn` also degenerates and it's unclear which norm it expects. `original` degenerating is also notable since it's the baseline PhaseNet.
- **Output tables truncated** — the in-domain table is cut off after `neic` row and the cross-domain table is cut off after the `instance` row. Don't yet have the complete numbers for `neic`, `obs`, `volpick`, `jma`, `jma_wc`, `scedc`, `iquique`, `lendb`, and `geofon` cross-domain metrics. These may be in the saved CSV but weren't captured in the notebook print output.
- **30,700 / 32,144 traces missing `source_month`** (~95.5%) — severely limits any time-stratified analysis.

---

## 🚧 Blockers & open questions

- **Degenerate model root cause unknown:** Why do `diting`, `original`, and `phasenet_sn` produce flat output? Possible causes: (1) norm mismatch — `diting` uses `std` but maybe Step 2 preprocessing didn't apply it correctly; (2) weight loading failure silently producing untrained random/zero weights; (3) SeisBench wrapper incompatibility for these specific weights. Need to isolate one trace, run it manually through each degenerate model, and inspect the raw output tensor.
- **Truncated metric table:** Full cross-domain results for most Tier B/C/D models are not yet visible. Need to inspect `step3_metrics.csv` directly and produce a complete summary table.
- **`neic` in-domain row truncated** — n_traces = 1,444 shown but p_med_prob, p_mae_s, mcc not captured.
- **S-phase cross-domain metrics for most models unknown** — s_mae_s = 1.660 s for `instance` is close to the outlier threshold; need full table to assess whether this is typical or an outlier.
- **PNW dataset (2,333 traces) performance not yet disaggregated** — this is the most relevant dataset for the Cascadia/PNSN context and deserves its own breakdown.

---

## 📅 Tomorrow's priority

**Primary:** Load `step3_metrics.csv` directly and produce a complete, clean summary table of all 16 models across all splits — especially cross-domain P- and S-MAE, recall, MCC, and outlier rate.

**Supporting tasks:**
1. Debug the three degenerate models (`diting`, `original`, `phasenet_sn`) — run a single trace manually, inspect raw output tensors, and confirm whether it's a norm mismatch or a weight-loading failure.
2. Disaggregate cross-domain metrics by dataset (especially `pnw`) and by distance bin if distance metadata is available — this is key for the benchmark paper framing.

---

## 🏷️ Topics

PhaseNet, SeisBench, benchmark, waveform-processing, recall-analysis, Cascadia-HPC, PNSN

---
*Auto-generated · [GitHub](https://github.com/Akashkharita)*
