# 2026-06-01

## 🔬 What I tested / ran

- Built and iterated on an automated online work logger (`dev-notes` repo) to track research progress in a structured, organized way — this involved 13+ commits across the day fixing GitHub Actions workflows, repo API parameters, PAT-based private repo access, and UI/serving issues.
- Integrated Claude Code with the remote Cascadia HPC machine for AI-assisted development in the remote environment.
- Added a presentation slides folder to the `Denolle-Lab/phasenet-retrain` repository (commit `701c059`) containing the latest project updates.
- Updated the benchmark dataset notebook (`notebooks/04_creating_benchmark_dataset.ipynb`) in `phasenet-retrain` (commit `7268af2`), enumerating available pretrained model weights and auditing benchmark datasets.
- Enumerated all available pretrained weights for **PhaseNet** (16 variants: `diting`, `ethz`, `geofon`, `instance`, `iquique`, `jma`, `jma_wc`, `lendb`, `neic`, `obs`, `original`, `phasenet_sn`, `pisdl`, `scedc`, `stead`, `volpick`) and **EQTransformer** (12 variants: `ethz`, `geofon`, `instance`, `iquique`, `lendb`, `neic`, `obs`, `original`, `original_nonconservative`, `scedc`, `stead`, `volpick`).
- Audited trace counts and P/S arrival coverage across 12+ SeisBench datasets.
- Generated and saved distribution plots for magnitude, depth, and distance bins across datasets → `physics_distributions.png`.

---

## 📊 Results & findings

**Dataset trace counts (verified in cache):**

| Dataset | Total Traces | Has P | Has S | Both | % Both |
|---|---|---|---|---|---|
| stead | 1,265,657 | 1,030,231 | 1,030,231 | 1,030,231 | 81.4% |
| ceed | 4,140,530 | 4,140,530 | 4,140,530 | 4,140,530 | 100.0% |
| instancecounts | 1,159,249 | 1,159,249 | 713,883 | 713,883 | 61.6% |
| pnw | 183,909 | 183,909 | 183,909 | 183,909 | 100.0% |
| txed | 519,689 | 312,231 | 312,2xx | — | — |
| ethz | 36,743 | — | — | — | — |
| obst2024 | 60,394 | — | — | — | — |
| mlaapde | 228,256 | — | — | — | — |
| vcseis | 160,278 | — | — | — | — |
| cwa | 346,959 | — | — | — | — |
| aq2009gm | 258,984 | — | — | — | — |
| geofon | 275,274 | — | — | — | — |

**Dataset sizes on disk (notable):**
- `crew`: 1,151.9 GB
- `cwa`: 185.1 GB
- `ceed`: 616.6 GB
- `instancecounts`: 168.1 GB
- `aq2009gm`: 28.1 GB
- `ethz`: 23.3 GB
- `geofon`: 27.8 GB
- `dummydataset`: 0.0 GB

**Cache status:** `pisdl` skipped (not in cache); all other listed datasets loaded OK.

**Binning scheme defined for benchmark evaluation:**
- Magnitude bins: [-2, -1, 0, 1, 2, 3, 4, 5, 6, 7, 10]
- Depth bins (km): [0, 5, 10, 20, 35, 70, 150, 300, 700]
- Distance bins (km): [0, 10, 30, 100, 300, 1000, 5000, 20000]

**stead magnitude distribution (sample):** 242 traces at M[-2,-1], 347,189 at M[0,1], 411,465 at M[1,2], 167,233 at M[2,3], 80,156 at M[3,4], 21,911 at M[4,5], 1,889 at M[5,6], 143 at M[6,7], 3 at M[7,10].

**Physics distribution figures** generated and saved: `physics_distributions.png` (1800×500 and 1800×600 figure sizes produced).

---

## 💡 Hypotheses & interpretations

- The large variation in P/S arrival completeness across datasets (61.6% for `instancecounts` vs. 100% for `ceed` and `pnw`) will matter significantly for benchmark design — models evaluated on S-phase detection need datasets that actually have S labels, and `instancecounts` will undersample that evaluation.
- `ceed` is by far the largest dataset at 4.14M traces with 100% P+S coverage, making it a potentially dominant training source; care should be taken that it doesn't overwhelm the benchmark with a single regional character (Chinese seismicity, DiTing provenance).
- The magnitude distribution in `stead` is heavily concentrated in M1–3 (the bulk of 347K + 411K traces), which is typical of regional catalogs — benchmark results will reflect this class imbalance unless stratified sampling is applied.
- Distance bins spanning 0–20,000 km are designed to capture both local and teleseismic regimes, consistent with the project's goal of testing PhaseNet generalization across distance classes.

---

## ✅ Decisions made and why

- **Built the `dev-notes` automated work logger** — decided to invest time in research infrastructure to enable better longitudinal tracking of progress. As stated: *"I worked on creating this online work logger to track my research progress in a better and organized way."*
- **Integrated Claude Code with Cascadia** — decided to use AI-assisted coding directly on the remote HPC machine to streamline development without context-switching.
- **Added slides to `phasenet-retrain`** — decided to version-control presentation materials alongside the code/notebooks so that the repository serves as a single source of truth for project state.
- **Defined specific magnitude/depth/distance bin edges** in the benchmark notebook — these are now committed as the canonical binning scheme for dataset evaluation and cross-model comparison.

---

## ❌ What failed / surprised

Nothing today. As stated: *"Nothing today."*

*(Note: the 13+ commits on `dev-notes` across the day, including fixes for repo API params, PAT authentication, branch merges, and serving issues, suggest the workflow setup was iterative and required debugging, even if nothing was characterized as a hard failure.)*

---

## 🚧 Blockers & open questions

- `pisdl` dataset is not in cache on Cascadia — needs to be downloaded before it can be included in benchmark evaluation.
- `txed` P/S counts appear to have been cut off in the notebook output — full coverage statistics for that dataset are not yet confirmed.
- Several datasets (e.g., `mlaapde`, `vcseis`, `obst2024`) have trace counts but P/S arrival completeness stats were not shown in the output — unclear whether those are fully audited yet.
- The `crew` dataset at 1,151.9 GB is the largest by disk footprint — unclear whether it's been fully loaded/verified or if storage on Cascadia is a concern.

---

## 📅 Tomorrow's priority

**Primary:** Begin evaluation of the benchmark datasets — run PhaseNet pretrained model variants against the assembled datasets and compute per-bin (magnitude/depth/distance) detection metrics.

**Supporting tasks:**
1. Confirm and fill in missing P/S arrival statistics for `txed`, `mlaapde`, `vcseis`, and `obst2024`.
2. Download/restore `pisdl` to cache so it can be included in the evaluation.
3. Set up stratified sampling if needed to handle magnitude-bin imbalance before running model evaluations.

---

## 🏷️ Topics

`PhaseNet`, `SeisBench`, `benchmark`, `waveform-processing`, `dataset-download`, `Cascadia-HPC`, `recall-analysis`, `visualization`, `phasenet-retrain`

---

**TL;DR:** Built a structured automated research logger and integrated Claude Code on Cascadia, while auditing 12+ SeisBench benchmark datasets (up to 4.14M traces) and defining the canonical binning scheme for upcoming PhaseNet generalization evaluation.

---
*Auto-generated · [GitHub](https://github.com/Akashkharita)*
